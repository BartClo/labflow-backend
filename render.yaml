version: "1"
projects:
- name: My project
  environments:
  - name: Production
    databases:
    - name: labflow-db-IgnacioS
      databaseName: labflow_db_ignacios
      user: labflow_db_ignacios_user
      plan: free
      region: virginia
      ipAllowList:
      - source: 0.0.0.0/0
        description: everywhere
      postgresMajorVersion: "18"
    services:
    - type: web
      name: labflow-backend
      runtime: docker
      region: virginia
      plan: free
      dockerfilePath: ./Dockerfile
      envVars:
      - key: JAVA_OPTS
        value: "-Xmx512m -Xms256m"
      - key: SPRING_PROFILES_ACTIVE
        value: prod
      - key: DATABASE_URL
        fromDatabase:
          name: labflow-db-IgnacioS
          property: connectionString
      - key: SWAGGER_ENABLED
        value: "true"
      - key: LOG_LEVEL
        value: INFO
